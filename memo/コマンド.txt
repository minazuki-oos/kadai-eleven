$ cd c:/                             git show コミットID
$ mkdir projects              
$ cd projects        
$ pwd
 mkdir github-handson
$ cd github-handson
$ echo "# github-handson" >> README.md
$ ls
$ cat README.md
git init
$ git branch -M main　ブランチを変更　mainになる。
$ git status
$ git add .
$ git commit -m "README.mdを追加"
 git log  qキーをおしてエンターを押すとぬけれる。
git push origin main -u
git push -u origin ブランチ名 　　　　ブランチのひもづけ